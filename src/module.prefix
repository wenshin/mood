/* This hack will break requirejs optimizer
({ define: typeof define === 'function'
  ? define  // browser require.js
  : function(fn) { fn(require,exports,module); } }).  // Node.js */
define(function (require, exports, module) {
